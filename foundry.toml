[profile.default]
src = "src"
out = "out"
libs = ["lib"]

auto_detect_remappings = false # remappings seem to cause problems
optimizer = true
optimizer_runs = 200
# via_ir = true
gas_limit = "18446744073709551615"
fuzz.gas_report_samples = 64       # gas report doesn't need so many test cases
ffi = true                         # needed for atomic file writes (mv command)

deny = "warnings"


remappings = [
  "ds-test/=lib/forge-std/lib/ds-test/src/",
  "forge-std/=lib/forge-std/src/",
  "@openzeppelin/contracts/=lib/openzeppelin-contracts-upgradeable/lib/openzeppelin-contracts/contracts/",
  "@openzeppelin/contracts-upgradeable/=lib/openzeppelin-contracts-upgradeable/contracts/",
  # OZ 5.6.x deletes ReentrancyGuardTransientUpgradeable, and 5.7.0 reduces UUPSUpgradeable to a
  # re-export of the non-upgradeable core (dropping Initializable from its bases and the empty
  # __UUPSUpgradeable_init shims). Redirect those import PATHS to byte-identical @bao shims, so
  # deployed sources keep the exact OZ import they were audited with.
  # The path is what must be redirected, not the import: verify-audit compiles a revision's source
  # against HEAD's foundry.toml, so a remapping reaches old revisions and an edited import cannot.
  # Longest-prefix match wins over the broader contracts-upgradeable/ rule above.
  "@openzeppelin/contracts-upgradeable/utils/ReentrancyGuardTransientUpgradeable.sol=src/openzeppelin-compat/ReentrancyGuardTransientUpgradeable.sol",
  "@openzeppelin/contracts-upgradeable/proxy/utils/UUPSUpgradeable.sol=src/openzeppelin-compat/UUPSUpgradeable.sol",
  "@solady/=lib/solady/src/",
  "openzeppelin-foundry-upgrades/=lib/openzeppelin-foundry-upgrades/src/",
  "solidity-stringutils/=lib/openzeppelin-foundry-upgrades/lib/solidity-stringutils/",
  "@bao/=src/",
  "@bao-test/=test/",
  "@bao-script/=script/",
  "@bao-factory/=lib/bao-factory/src/",
  "@bao-factory-test/=lib/bao-factory/test/",
  "src/=src/",
  "test/=test/",
]


fs_permissions = [
  { access = "read", path = "./" },
  { access = "read-write", path = "./results" },
  { access = "read-write", path = "./deployments" },
  { access = "read-write", path = "./tmp" },
]

#ignored_error_codes=["transient-storage"]
ignored_warnings_from = [
  "lib/openzeppelin-contracts-upgradeable/lib/openzeppelin-contracts/contracts/utils/StorageSlot.sol",
]

gas_reports_ignore = [
  "ERC20Mock",
  "ERC1967Proxy",
  "DerivedBaoOwnable",
  "DerivedBaoOwnableRoles",
  "DerivedBaoOwnableRoles_v2",
  "DerivedBaoOwnableTransferrable",
  "DerivedBaoOwnableTransferrableRoles",
  "DerivedBaoOwnable_v2",
  "DerivedOwnable",
  "DerivedOwnableRoles",
  "DerivedTokenHolder",
  "BaoOwnableAdapter",
  "MockImplementationWithState",
]

[lint]
exclude_lints = [
  "mixed-case-function",
  "mixed-case-variable",
  "screaming-snake-case-constant",
]
severity = ["gas"]

[etherscan]
mainnet = { key = "${ETHERSCAN_KEY}" }

[rpc_endpoints]
sepolia = "${SEPOLIA_RPC_URL}"
mainnet = "${MAINNET_RPC_URL}"
local = "http://localhost:8545"


# See more config options https://github.com/foundry-rs/foundry/blob/master/crates/config/README.md#all-options
